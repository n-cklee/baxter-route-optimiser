# Virtual environment
venv/
.venv/
env/

# Python bytecode
__pycache__/
*.py[cod]

# macOS
.DS_Store

# Claude Code settings
.claude/

# Local data files (upload via the app, don't commit patient data)
data/*.xlsx
data/*.xls
data/*.csv

# OSRM matrix cache — date-specific, not committed
cache/osrm_*.json
